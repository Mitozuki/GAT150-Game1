#include "ModelRenderComponent.h"

namespace kiko
{
	void kiko::ModelRenderComponent::Update(float df)
	{

	}

	void kiko::ModelRenderComponent::Draw(Renderer& renderer)
	{
		// Model->Draw(renderer, m_owner->m_transform);
	}
}
